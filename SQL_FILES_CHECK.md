# SQL Files Status Check

✅ **SQL files are already properly supported in incognide file browser**

- `.sql` files are included in `commonFileTypes` with icon 🗃️ and label "SQL"
- No exclusion for `.sql` in `excludedExtensions`
- File filtering logic allows SQL files to display normally
- SQL files should already appear in the sidebar file browser

This branch serves to verify SQL file visibility is working as expected.