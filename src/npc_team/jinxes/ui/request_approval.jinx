jinx_name: request_approval
description: Request user approval before performing an action
permissions:
  default: allow
inputs:
  - message:
      type: string
      description: The approval message to display
      required: true
steps:
  - name: request_approval
    engine: incognide
    action: confirm
    args:
      message: "{{ message }}"
